class MandrillService
  class << self

    def message(lead)
    # TODO: return
    #   {
    #       subject: subject,
    #       to: [{ email: to }],
    #       merge_vars: [{ rcpt: to, vars: merge_vars }]
    #   }
    end
  end
end
